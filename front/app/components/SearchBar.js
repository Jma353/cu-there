import React, { Component } from 'react'

/**
 * Search bar (the search input itself).
 */
class SearchBar extends Component {
  render () {
    return (
      <div />
    )
  }
}

export default SearchBar
